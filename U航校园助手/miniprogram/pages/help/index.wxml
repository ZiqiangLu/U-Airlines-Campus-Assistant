<view class="page">
  <view class="help-head panel">
    <view>
      <view class="title">悬赏互助</view>
      <view class="muted small">代取、维修、学习互助等内容独立展示。</view>
    </view>
    <button class="publish-btn" bindtap="goPublish">发布</button>
  </view>

  <view class="chip-row category-row">
    <view
      wx:for="{{categories}}"
      wx:key="key"
      class="chip {{activeCategory === item.key ? 'active' : ''}}"
      data-key="{{item.key}}"
      bindtap="selectCategory"
    >
      {{item.name}}
    </view>
  </view>

  <view wx:if="{{posts.length}}">
    <post-card
      wx:for="{{posts}}"
      wx:key="_id"
      post="{{item}}"
      compact="{{true}}"
      bind:open="openDetail"
    />
  </view>

  <view wx:elif="{{!loading}}" class="empty">暂时没有悬赏互助信息。</view>
  <view wx:if="{{loading}}" class="loading">加载中...</view>
</view>
